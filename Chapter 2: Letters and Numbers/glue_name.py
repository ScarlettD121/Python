#Scarlett Dowdle
#September 14, 2026
#DESCRIPTION: Outputs a message based on name inputs stored as varibles to the console


f_name = input("Enter first name: ") #get user's first name as a string
l_name = input("Enter last name: ") #get user's last name as a string

print(f"Hi, {f_name} {l_name}") # expected output "Hi, [user's first name] [user's last name]"
